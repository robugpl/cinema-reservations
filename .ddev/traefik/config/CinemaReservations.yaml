#ddev-generated
# If you remove the ddev-generated line above you
# are responsible for maintaining this file. DDEV will not then
# update it, for example if you add `additional_hostnames`, etc.

http:
  routers:
    CinemaReservations-web-80-http:
      entrypoints:
        - http-80
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-web-80"
      tls: false
      # middlewares:
      #   - "CinemaReservations-redirectHttps"
    CinemaReservations-web-8025-http:
      entrypoints:
        - http-8025
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-web-8025"
      tls: false
      # middlewares:
      #   - "CinemaReservations-redirectHttps"
    CinemaReservations-xhgui-80-http:
      entrypoints:
        - http-8143
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-xhgui-80"
      tls: false
      # middlewares:
      #   - "CinemaReservations-redirectHttps"
    
    
    
    CinemaReservations-web-80-https:
      entrypoints:
        - http-443
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-web-80"
      
      tls: true
      
    CinemaReservations-web-8025-https:
      entrypoints:
        - http-8026
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-web-8025"
      
      tls: true
      
    
    CinemaReservations-xhgui-80-https:
      entrypoints:
        - http-8142
      rule: HostRegexp(`^cinemareservations\.ddev\.site$`)
      
      service: "CinemaReservations-xhgui-80"
      
      tls: true
      
    

  middlewares:
    CinemaReservations-redirectHttps:
      redirectScheme:
        scheme: https
        permanent: true

  services:
    CinemaReservations-web-80:
      loadbalancer:
        servers:
          - url: http://ddev-CinemaReservations-web:80
        
    CinemaReservations-web-8025:
      loadbalancer:
        servers:
          - url: http://ddev-CinemaReservations-web:8025
        
    
    
    CinemaReservations-xhgui-80:
      loadbalancer:
        servers:
          - url: http://ddev-CinemaReservations-xhgui:80
        
    
    

tls:
  certificates:
    - certFile: /mnt/ddev-global-cache/traefik/certs/CinemaReservations.crt
      keyFile: /mnt/ddev-global-cache/traefik/certs/CinemaReservations.key